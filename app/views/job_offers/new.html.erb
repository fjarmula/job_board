<div class="page-header">
  <h1 class="page-title"><%= @job_offer.persisted? ? "Edit Job Offer" : "Create New Job Offer" %></h1>
</div>

<div class="content-card">
  <%= render 'job_offers/form', job_offer: @job_offer %>
</div>
