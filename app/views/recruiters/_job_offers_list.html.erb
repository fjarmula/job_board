<div>
  <% if recruiter.job_offers.any? %>
    <div class="job-offers-grid">
      <%= render recruiter.job_offers, current_recruiter: current_recruiter %>
    </div>
  <% else %>
    <p style="color: var(--text-muted); font-style: italic;">No job offers posted yet.</p>
  <% end %>
</div>
